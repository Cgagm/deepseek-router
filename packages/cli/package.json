{
  "name": "@deepseek-router/cli",
  "version": "1.0.0",
  "description": "CLI entry point for DeepSeek Router — start the proxy with one command",
  "license": "MIT",
  "type": "module",
  "bin": {
    "deepseek-router": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "@deepseek-router/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "typescript": "^5.4.0"
  }
}
